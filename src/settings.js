// Note that some of these settings should also correspond to the settings in the SCSS params file.
const settings = {
	numFlags: 9, // The number of flags we use.
	numColors: 6, // The number of colors we use in the game.
	numNumbers: 9, // The number of numbers there are for each color.
	numCardsInHand: 6, // The number of cards a player can have in hand.
}

export default settings